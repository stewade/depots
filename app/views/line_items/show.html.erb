
<% if notice %>
  <aside id="notice"><%= notice %></aside>
<% end %>

<h2>Your Pragmatic Cart</h2>
<ul>
  <% @cart.line_items.each do |item| %>
    <li><%= item.try(:quantity) %> &times; <%= item.product.try(:title) %></li>
  <% end %>
</ul>

